{
  "scripts": {
    "docs:dev": "vitepress dev .",
    "docs:build": "if [ \"$(git rev-parse --is-shallow-repository)\" = true ]; then git fetch --unshallow; fi && vitepress build .",
    "docs:preview": "vitepress preview ."
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
